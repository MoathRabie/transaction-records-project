# transaction-records-project
This project merges transaction records from CRM and ERP systems into one dataset, then detects duplicates — exact, partial, cross-system, temporal, and fuzzy — using composite keys and transaction ID counts. Each row gets a duplication risk rating (Clean/High Risk), producing a final deduplicated dataset ready for clean reporting.
